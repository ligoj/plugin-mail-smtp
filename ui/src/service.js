/*
 * Service layer for plugin "mail-smtp". The legacy `smtp.js` controller
 * was empty — the SMTP tool contributes no row actions or detail chips.
 * Its entire UI surface is the parameter labels shipped via i18n. Kept
 * empty for contract parity.
 */
const service = {}

export default service
