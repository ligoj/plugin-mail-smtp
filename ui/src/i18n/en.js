// SMTP-specific i18n for the mail-smtp tool plugin: node parameter labels.
// The legacy `smtp.js` controller was empty, so these labels are the
// plugin's entire UI contribution.
export default {
  'service:mail:smtp': 'SMTP',
  'service:mail:smtp:host': 'Host',
  'service:mail:smtp:port': 'Port',
  'service:mail:smtp:username': 'Username',
  'service:mail:smtp:password': 'Password',
}
